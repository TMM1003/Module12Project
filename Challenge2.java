public class Challenge2 {

    public static void main(String[] args) {
        // TODO Keep this file small and use it only to connect the other classes.
        // Suggested call order after each teammate finishes their file:
        // 1. Thomas: launchGui()
        // 2. Thomas: collectUserInputFromGui()
        // 3. Kang: turnProjectIntoGitRepo(...)
        // 4. Kang: addGitIgnoreFile(...)
        // 5. Kang: addReadMeFile(...)
        // 6. Jordan: createInitialCommit(...)
        // 7. Jordan: createGitHubRepoMirror(...)
        // 8. Jordan: setOriginRemote(...)
        // 9. Kenneth: pushInitialCommit(...)
        // 10. Kenneth: giveUserRepoUrl(...)
        // 11. Thomas: showRepoUrlInGui(...)
    }
}
